@model IEnumerable<string>
@{
    ViewData["Title"] = "Values";
    
}

<div class="container">
<h1>@ViewData["Title"]</h1>
    <ul>
        @foreach (var value in Model)
        {
            <li>@value</li>
        }
    </ul>
</div>